## No Requirement Changes

This change is purely implementation wiring and configuration:

- Existing Serena MCP configuration is being corrected (missing `Makefile` recipes are added).
- Host ports are changed to avoid collision with another local project.
- No behavioral requirements of the lilt CLI or its public interface are modified.

Therefore, no ADDED, MODIFIED, REMOVED, or RENAMED requirements are specified.
